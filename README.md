https://github.com/rvHoney/episcinePas de beau README dsl

Faut mettre le **SCRIPT** `episcine` dans `/usr/bin` et le **DOSSIER** `episcine` dans `/usr/share` sans le script.

Initialiser:
`episcine init <tag> <files>`

Push
`episcine push`
